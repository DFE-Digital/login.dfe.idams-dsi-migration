name: $(BuildDefinitionName)_tag-$(BuildID)_at-$(Date:yyyyMMdd)$(Rev:.r)
resources:
  repositories:
    - repository: devopsTemplates
      type: github
      endpoint: DfE-Digital
      name: DFE-Digital/login.dfe.idams-dsi-migration
      ref: master
trigger:
- master

pool:
  vmImage: 'windows-latest'

variables:
- group: platform-global
- group: platform-dev
- group: platform-dev-idamsdsiapp
stages:
 - template: /templates/functionapps/stages/pirean-master-functionapp-stage.yml@devopsTemplates
   parameters:
     applicationName: idamsdsiapp
    
      